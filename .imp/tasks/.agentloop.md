mut loop = 0; while $loop < 10 { opencode run -m "anthropic/claude-opus-4-5" "READ all of `.imp/tasks/task-08A.md`. Pick ONE task to do, verify in git history. Verify via grep/glob search in codebase or web/code search. Complete task, verify via clippy and tests. Commit change. ONLY do one task. Update/tick off in the task file. If you learn a new, undocumented critical operational detail (e.g. how to build, test, unintuitive designs, issues, blockers, etc.), update or add to the `# DEV NOTES` section in AGENTS.md. If all tasks done, sleep 5s and exit. NEVER GIT PUSH. ONLY COMMIT."; }
