/// Marker type for the grammars domain package.
///
/// Grammars are schema-only artifacts loaded from compiled assets and consumed by
/// downstream language build/runtime systems.
pub struct Grammars;
