# Named snippets available for :snippet @name.
{
  snippets: [
    { common: { name: fori, description: "for-loop with index", keys: [forloop] }, body: "for ${1:i} in 0..${2:n} {\n\t$0\n}" }
    { common: { name: ifelse, description: "if/else block", keys: [if] }, body: "if ${1:cond} {\n\t$0\n} else {\n\t\n}" }
  ]
}
